node_modules/
.env
.env.local
.env.*.local

# Ad-hoc credential scratch files — never commit these.
DB_password
*.secret

# Generated Prisma client — regenerated by `npm run generate`. Not committed
# so dev + CI always rebuild against the current schema.
prisma/generated/

# whatsapp-web.js LocalAuth profiles — Chromium user-data dirs. The worker
# tars these into Postgres for persistence; the on-disk copy is ephemeral.
.wwebjs_auth/
.wwebjs_cache/

# tsx incremental cache
.tsbuildinfo

# Editor / OS
.DS_Store
*.log
